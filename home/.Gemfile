# -*- mode: ruby -*-
#
# System-wide Gemfile.
#
# Usage
# ```
# bundle install --system --gemfile=~/.Gemfile
# ```
source 'https://rubygems.org'

gem 'ruby-lint'
gem 'scss_lint'
gem 'scss_lint_reporter_checkstyle'
